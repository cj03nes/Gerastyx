
pragma solidity ^0.8;


import Zeropoint from "./zeropoint.sol";
// zeropoint + color, opacity, strength, etc
// place between highway medians & exits to create anti-crash speed lanes
// 

// anti-collision & anti-terrorism

contract ZeropointShield ={

opacity: 0.5;
position: absolute solid;
color: #739BD0;
outline: thick double #32a1ce;
visibility: visible;


async function createForcefield ={
     (require == zeropoint);
     (require == zeropointWifi);
encapsulate powerBox{
powerBox = zeropoint(from);  
encapsulate = surround(); } }

if xCar = collision, then return reboundCushion,
else if xCar = !collision then return absolute solid;

reboundCushion = absolute solid + catch(object);

console.log(ZeropointShield = absolute solid reboundCushion catch);



}

